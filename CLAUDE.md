# CalDrop 開発ガイド

## プロジェクト概要
自由な書き方のテキストから予定を解析し、Googleカレンダーに一括登録できるWebツール。

## 開発者プロフィール
- Python 経験者。HTML / CSS / JavaScript はほぼ未経験
- 実装時は「なぜそう書くのか」「Python との違いは何か」など解説を挟みながら進める
- 専門用語には補足説明を付ける

## 基本ルール
- チャットは全て日本語で行うこと
- コードのコメントも日本語で書くこと
- 不明点は必ず確認してから実装すること
- 実装前に何をするか説明してから進めること
- コードの変更点は Python との比較や背景を交えて解説すること

## 技術スタック
- HTML / CSS / JavaScript（バニラJS）
- サーバーなし・APIなし・完全フロントエンドで動作
- ホスティング：Vercel（GitHub連携で自動デプロイ）

## ディレクトリ構成
```
caldrop/
├── index.html              # メインページ
├── README.md               # プロジェクト説明
├── CLAUDE.md               # このファイル
├── .gitignore
├── css/
│   └── style.css           # スタイル（ダークモード対応）
├── js/
│   ├── config.js           # クライアントID（承認済みオリジン保護のためGit管理内）
│   ├── config.example.js   # config.js のサンプル
│   ├── parser.js           # テキスト解析ロジック
│   ├── gcal.js             # Google Calendar URL 生成・終了時刻計算ヘルパー
│   ├── gcal-api.js         # Google Calendar API 連携（OAuth・一括登録）
│   ├── ui.js               # 画面描画・操作
│   └── main.js             # エントリーポイント
└── test/
    └── parser.test.js      # 解析ロジックのユニットテスト
```

## テスト
- 解析ロジック（parser.js / gcal.js の純粋関数）は Node.js 標準テストランナーでテストする
- 実行：プロジェクト直下で `node --test`
- parser.js / gcal.js の末尾に `module.exports` ガードがあり、ブラウザ・Node 両対応
- **解析ロジックを変更したら必ずテストを実行し、新パターン追加時はテストも追加すること**

## 主な機能
- 日本語の自由なテキストから日付・時刻・タイトル・場所・説明を解析
- 複数予定の一括解析（1行1予定）
- ライブプレビュー（入力が止まると自動で解析・カード表示）
- チェックボックスで登録する予定を選択可能
- 「Googleカレンダーに登録」で全予定を一括登録（Google Calendar API 使用）
- 個別の「＋ 個別追加」リンクで1件ずつ URL 経由登録も可能
- 解析後のカードをその場で編集（タイトル・日付・時刻・場所・説明）
- 入力順 / 日付順のソート切り替え
- Cmd / Ctrl + Enter で解析を実行（PC）
- 入力ファーストのレイアウト（テキストエリアが最上部・使い方は折りたたみで下部）

## 対応している入力形式
```
8月1日 10:00-15:00 散髪
8/3 15時から17時 ジム
8/20-22 夏休み旅行
8月10日 休み
今日 15:00 歯医者
明日 10:00-11:00 打ち合わせ @渋谷オフィス
9/10 結婚式 会場：ホテルグランド （スピーチあり）
2025/4/5 14:00 過去の予定（年を明示）
```

### 日付指定
- `4月20日` / `4/20` — 漢字・スラッシュ両対応
- `4/25-27` — 日付範囲（複数日終日イベント）
- `今日` / `明日` / `明後日` — 相対日付（行の途中でも認識。「明日香」などの誤検出は否定先読みで回避）
- 本日より前の日付は自動的に翌年に補正
- `2025/4/5` / `2025年4月6日` — 年を明示すると補正をスキップ（過去の予定登録に使用）
- 実在しない日付（13月・45日）・時刻（25時）は弾く（その行は警告表示）

### 時刻の区切り文字
`-` `〜` `~` `～` `ー` `から` および半角・全角スペース すべて対応。
開始時刻のみ指定した場合、終了時刻は1時間後に自動設定（Googleカレンダーのデフォルトに準拠）。
終了時刻が開始時刻以前の行（`17時から15時` など）は誤記とみなし解析失敗（警告表示）。
日またぎの予定（`22時から2時`）は現状非対応（今後の課題）。

### 場所の指定方法（3種類）
- `@会議室A` — @マークに続けて記述（半角 `@`・全角 `＠` 両対応）
- `場所：新宿オフィス` / `会場：ホテルXX` / `集合：渋谷駅` など — キーワード＋コロン
- `東京` `大阪` `京都` など主要都市名 — 単語単独でも検出（フォールバック）

### 説明の指定方法
- `（持ち物：PC・名刺）` / `(事前にレポート提出)` — 丸括弧内のテキスト

## デザイン思想

### コンセプト
「メモ帳に書く感覚」で使えるツール。装飾を排除し、入力とアクションに集中できるミニマルな UI を目指した。

### 参考にしたデザインシステム
- **[Notion](https://www.notion.so/)** — テキストカラー（`#37352f`）・サブテキスト（`#787774`）・背景（`#f8f7f4`）の暖色グレー体系を参考。純白・純黒を避けた柔らかさが特徴。
- **[GitHub Primer](https://primer.style/)** — 意味色（success / info / danger / warn）の色設計を参考。
- **[Radix UI](https://www.radix-ui.com/colors)** — ダークモードの意味色トーンを参考。

### カラーパレット

| 変数 | ライト | ダーク | 用途 |
|------|--------|--------|------|
| `--bg` | `#f8f7f4` | `#1b1b19` | ページ背景 |
| `--surface` | `#ffffff` | `#242420` | カード・入力欄 |
| `--border` | `#e6e5df` | `#383834` | 枠線 |
| `--text` | `#37352f` | `#eeede6` | 本文 |
| `--text-sub` | `#787774` | `#8a8982` | 補足・ラベル |
| `--accent` | `#37352f` | `#eeede6` | プライマリボタン背景 |

意味色（success / info / danger / warn）は背景色と文字色のペアで定義し、ライト・ダーク両対応。

### ロゴ
- `CalDrop` テキスト + グリーンの `.`（`#2e9e5e`）のみ
- 「.」は「ドロップ（Drop）」と「入力の手軽さ（メモの終止符）」を表現

### フォント
- UI 全体：システムフォント（`-apple-system`, `Hiragino Sans`, `Yu Gothic Medium`, `Meiryo`）
  - Yu Gothic（Regular）は Windows で極端に細く描画されるため Medium を優先
- テキストエリア・コードブロック：等幅フォント（`SF Mono`, `Menlo`, `Consolas`）

### アクセシビリティ・スマホ対応
- キーボード操作時のみ `:focus-visible` でフォーカスリングを表示
- チェックボックスに `aria-label`、装飾絵文字に `aria-hidden` を付与
- スマホ（480px以下）：入力欄は16px（iOS の自動ズーム防止）、カードのボタン列は下の行に折り返し
- 自動フォーカスはホバー可能な端末（PC）のみ（スマホでキーボードが勝手に開くのを防ぐ）

## Google Calendar API 設定
- Google Cloud Console でプロジェクト作成済み
- OAuth クライアントID を `js/config.js` に記載
- config.js はフロントエンドのみで使用し、承認済み JavaScript 生成元（オリジン）による保護があるため Git 管理内で問題なし
- 承認済み JavaScript 生成元：`http://127.0.0.1:5500`（ローカル）、Vercel URL（本番）
- アプリは「テスト」モード。他ユーザーに公開するときのみ「本番環境」への移行が必要
- テストユーザー：登録済み（非公開）

## ライブプレビューの設計
- 入力停止後0.6秒で自動解析（デバウンス）。手動の「解析する」ボタン・Cmd+Enter も併存
- チェック・編集・登録済みの状態は DOM ではなく `parsedEvents`（データ側）に持つ
- 各イベントは由来する行テキスト（`sourceLine`）を記録し、再解析時に同じ行があれば
  既存オブジェクトを再利用してカードの編集・チェック状態を保持する
- 行を書き換えるとその行のカードは作り直される（カード上の編集は破棄される仕様）
- 手動追加カード（`sourceLine: null`）は再解析の影響を受けず常に残る
- カード編集中・一括登録中（`isRegistering`）は自動再解析を保留する
- 自動解析ではカーソルがある行（入力途中の行）を「認識できない行」警告の対象外にする

## 認証の仕組み
- Google Identity Services（GIS）の OAuth トークンモデルを使用
- アクセストークンは1時間で失効（Google の仕様、変更不可）
- 2回目以降は `prompt: 'none'` でサイレント取得（ページ再読み込み後も認証画面なし）
- `localStorage` に認証済みフラグを保存。Googleにログイン中の限り永続的に動作
- スマホで `prompt:'none'` が応答を返さない場合、4秒タイムアウトで通常フローにフォールバック
- ポップアップブロック対策：ブラウザはクリック直後の短時間しかポップアップを許可しないため、
  自動再試行（タイムアウト後など）はブロックされることがある。
  `error_callback` の `popup_failed_to_open` で検出し、「Google認証を開く」ボタンを表示して
  ユーザーのクリック直下で再試行する（クリック直下なら必ず許可される）

## バージョン管理
- バージョン形式：`v0.x.x`（セマンティックバージョニング）
  - パッチ（`v0.1.0` → `v0.1.1`）：バグ修正・小さな調整
  - マイナー（`v0.1.0` → `v0.2.0`）：新機能追加・UIの大きな変更
- 記載場所：`index.html` のフッター内 `<span class="footer-ver">vX.X.X</span>`

## コミット前チェックリスト
コミットする際は必ず以下の3点を行うこと：
1. `index.html` のフッターのバージョン番号を更新する
2. `CLAUDE.md` に変更内容を追記する
3. `README.md` に変更内容を追記する

## デプロイ方法
```bash
git add .
git commit -m "変更内容"
git push
```
GitHubにpushするとVercelに自動反映される。

## 今後の実装予定
- 解析精度のさらなる向上
- 繰り返し予定への対応
- リマインダー設定
- OGP画像の設定
- セキュリティヘッダー（CSP）の設定・トークンの localStorage 保存見直し
- Google AdSenseの導入

## 注意事項
- ユーザーデータは一切保存しない
- サーバーサイドの処理は行わない
- 無料で運用できる構成を維持すること
